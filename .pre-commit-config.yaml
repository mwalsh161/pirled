repos:
  - repo: local
    hooks:
      - id: update-firmware-version
        name: Update firmware version hash
        entry: scripts/hooks/update-version.sh
        language: script
        always_run: true
        pass_filenames: false
        stages: [commit]
